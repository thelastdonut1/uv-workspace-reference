[project]
name = "summarizer"
version = "1.0.0"
description = "Library for summarizing text using OpenAI's API"
requires-python = ">=3.13"
dependencies = [
    "openai>=2.44.0",
]

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
